// Future hook: fetch MTGJSON SealedProduct data for a set.
// SealedProduct entries include purchaseUrls which can feed a stock-watcher
// that alerts when product becomes available or reprices.
export async function getProductsForSet(setCode) {
  return [];
}
